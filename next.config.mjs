/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['images.pexels.com'], // Pexels görsellerini güvenli bir şekilde yüklemek için domain'i ekliyoruz
    },
  };
  
  export default nextConfig;
  